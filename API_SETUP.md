# 🔧 Настройка API для режима "Интервью"

## 📋 Что сделано

### Backend (Serverless)
- ✅ `api/interview.py` — Python функция для Vercel
- ✅ `requirements.txt` — зависимость `openai`
- ✅ `vercel.json` — минимальная конфигурация

### Frontend
- ✅ `assets/js/interview.js` — интеграция с API
- ✅ `interview.html` — блоки для оценки и фидбека
- ✅ `assets/css/style.css` — стили для результата

---

## 🚀 Деплой на Vercel

### 1. Коммит и пуш
```bash
git add .
git commit -m "feat: добавлена GPT интеграция в режим Интервью"
git push origin main
```

### 2. Настройки в Vercel (уже сделано)
- ✅ `OPENAI_API_KEY` — ваш ключ от OpenAI
- ✅ `OPENAI_MODEL` — `gpt-4o-mini`

### 3. Deploy
- Нажмите кнопку **"Deploy"** в Vercel

---

## 🔍 Как работает

```
User → Frontend → POST /api/interview
                     ↓
                  Vercel Function
                     ↓
                  OpenAI API
                     ↓
                  JSON Response
                     ↓
                  Frontend (оценка + фидбек)
```

---

## 🧪 Тестирование

После деплоя:
1. Откройте сайт
2. Перейдите в режим **"Интервью"**
3. Выберите тему
4. Напишите ответ
5. Нажмите **"Отправить"**

Вы увидите:
- 🔄 **"Проверка..."** (пока GPT думает)
- 🎯 **ОЦЕНКА** (1-10 с цветом)
- 💬 **ФИДБЕК** (конструктивный комментарий)
- 📖 **ЭТАЛОННЫЙ ОТВЕТ** (для сравнения)

---

## 🎨 Цвета оценок

- 🟢 **8-10** — Зеленый (отлично)
- 🟠 **5-7** — Оранжевый (хорошо)
- 🔴 **1-4** — Красный (требует улучшения)

---

## ⚠️ Важно

- ✅ API ключ **НЕ виден** в коде фронтенда
- ✅ Запросы идут через **ваш backend**
- ✅ Ошибки обрабатываются корректно
- ✅ CORS настроен для всех запросов

---

## 🐛 Если что-то не работает

1. **Проверьте Vercel Logs:**
   - Vercel Dashboard → Your Project → Functions → Logs

2. **Проверьте ENV переменные:**
   - Vercel Dashboard → Settings → Environment Variables

3. **Проверьте браузерную консоль:**
   - F12 → Console (для ошибок на фронтенде)

---

## 📝 Промпт для GPT

Промпт формируется **только на сервере** и включает:
- Роль: опытный интервьюер Data Analyst/Data Scientist
- Задача: оценка по шкале 1-10
- Критерии: полнота, корректность, структурированность, применимость
- Формат ответа: строгий JSON

---

**Готово к использованию!** 🎉
