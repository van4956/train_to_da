# Train to DA

Статический веб-тренажёр для подготовки к собеседованиям на позицию DA / DS / ML.

Проект построен на основе Markdown-карточек из моего Obsidian, не требует сервера или базы данных.
Основная цель — системное повторение теории, активное вспоминание и симуляция интервью.


## Возможности

- Более 400+ вопросов по SQL, Python, ML, Алгоритмам, Статистике, A/B тестам и т.д.
- Фильтрация по темам и уровню сложности
- 4 режима повторения:
  - Каталог
  - Карточки
  - Блиц-проверка
  - Интервью (с LLM)
- Статический хостинг через GitHub Pages
- Генерация данных одной командой


## Структура проекта

```
.
├── database/
│ ├── Train to DA.md    # оглавление, описание тем
│ ├── №1.md
│ ├── №2.md
│ └── ...
├── build.py            # сборка data.json
├── data.json           # единый источник данных
├── index.html          # главная, режим каталог
├── cards.html          # режим карточек
├── blitz.html          # режим блиц
├── interview.html      # режим интервью
├── assets/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   ├── data.js     # загрузка data.json
│   │   ├── filters.js  # ильтрация по теме, по уровню
│   │   ├── catalog.js  # логика каталога, рендер карточек, счётчики
│   │   └── utils.js    # random, shuffle, форматирование
│   └── icons/
└── README.md
```


## Формат карточек

Каждая карточка это отдельный `.md` файл:

```
Вопрос

---

Ответ

---

#lvl_1
```

- номер карточки задаётся именем файла
- уровень сложности субъективный
- контент не зависит от UI



## Сборка данных

Для генерации `data.json`:

```bash
python build.py
```

- читает все `.md`
- парсит вопросы, ответы, теги
- формирует структуру данных для фронта



## Цель проекта

* систематизировать знания
* убрать зависимость от порядка конспектов
* тренировать вспоминание, а не чтение
* подготовиться к собеседованиям
