

Когда следует использовать `pathlib` вместо `os.path`?

---

`pathlib` стоит использовать в большинстве новых проектов, когда нужна удобная и безопасная работа с путями.

`os.path` чаще встречается в легаси-коде или низкоуровневых скриптах.

---

#lvl_2 