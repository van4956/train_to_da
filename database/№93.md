
Как получить TOP-N товаров в категории с помощью оконных функций? Если есть поля `category`, `product_id` и `revenue`.

---

Используем `ROW_NUMBER() OVER (PARTITION BY category ORDER BY revenue DESC)` и фильтруем по `rn <= N`

---

#lvl_2 