
Что вернет запрос?

```sql
SELECT 
	DATE_TRUNC('week', created_at) AS week,
    percentile_cont(0.9) WITHIN GROUP (ORDER BY response_minutes)
FROM tickets
GROUP BY 1;
```

---

Возвращает недельный 90-й перцентиль времени ответа.

---

#lvl_2 