

Как работает `Counter.most_common`?

---

Метод `most_common(n)` возвращает `n` элементов с наибольшей частотой из объекта `Counter` в виде пар `(элемент, количество)`. Элементы отсортированы по убыванию частоты.

```python
Counter('abbccc').most_common(2) # [('c', 3), ('b', 2)]
```

---

#lvl_2 