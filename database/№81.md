
Как безопасно удалить дубликаты, оставив одну запись?

---

Дубликаты нумеруют с помощью `ROW_NUMBER()` внутри группы одинаковых ключей, затем удаляют строки с номером больше 1. Такой подход позволяет явно контролировать, какая запись остаётся, и избежать случайного удаления.

---

#lvl_3 