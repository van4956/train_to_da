

Как работает `csv.DictWriter` при записи отчётов?

---

`csv.DictWriter` записывает CSV-файл из списка словарей, где ключи соответствуют именам столбцов. Полезен когда данные уже в виде словарей и важны имена колонок.

---

#lvl_2 