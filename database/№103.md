
Как объяснить работу `GROUPING SETS` простыми словами?

---

`GROUPING SETS` позволяют задать несколько комбинаций группировок в одном запросе вместо `UNION` — например, по стране, по устройству и по обоим параметрам сразу.

---

#lvl_2 