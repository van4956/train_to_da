

Что делает `asyncio.gather`?

---

`asyncio.gather()` запускает несколько корутин (управляемая функция, которая умеет ждать) конкурентно и ожидает их завершения, возвращая список результатов в порядке передачи.

---

#lvl_2 