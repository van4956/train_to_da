

Что происходит при использовании `Decimal` для денежных расчётов?

---

Тип `Decimal` хранит числа в точном десятичном виде, поэтому операции с денежными суммами выполняются без ошибок округления, характерных для `float`.

---

#lvl_2 